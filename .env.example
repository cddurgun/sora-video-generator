# Database Configuration (Local Development - SQLite)
DATABASE_PROVIDER="sqlite"
DATABASE_URL="file:./prisma/dev.db"

# Database Configuration (Vercel Production - PostgreSQL)
# For Vercel Postgres, add these variables in Vercel Project Settings:
# DATABASE_PROVIDER="postgresql"
# DATABASE_URL will be automatically provided by Vercel Postgres
# DIRECT_URL will be automatically provided by Vercel Postgres

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
# For Vercel, set to: NEXTAUTH_URL="https://your-project.vercel.app"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# OpenAI API Configuration
# Leave empty - API key is managed client-side via settings page
# For production, you may want to set up environment-based API key management
# NEXT_PUBLIC_OPENAI_API_KEY=sk-your-api-key-here
